# shared-c_car_platform

Shared models/schemas/utilities package (placeholder).

## Local development

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Commands (planned)
When Node.js package structure is added:

```bash
npm install
npm run lint
npm run format
npm test
```
